# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TensorLiteSamples-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'TensorFlowLiteSwift'
  
  # Pods for TensorLiteSamples-iOS
  
  target 'TensorLiteSamples-iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'TensorLiteSamples-iOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
end
